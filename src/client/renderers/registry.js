import registry from '@/src/shared/renderRegistry';

export const chromeRoleMap = registry.PIPELINE_CHROME_ATOMIC_ROLE;
export const bodyRoleMap = registry.PIPELINE_BODY_ATOMIC_ROLE;
export const renderableComponentIds = Array.from(registry.RENDERABLE_COMPONENT_IDS);
